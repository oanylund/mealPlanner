import React, { PropTypes } from 'react'
import BlazeTemplate from '../BlazeTemplate.jsx'

const addIngredientCategory = (props) => {
  return (
    <BlazeTemplate templateName='addIngredientCategory'/>
  )
}

export default addIngredientCategory
